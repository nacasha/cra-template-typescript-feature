/**
 * This file does not need further configurations.
 * Only modify this file when needed.
 */

import { createBrowserHistory } from 'history';

const browserHistory = createBrowserHistory();

export default browserHistory;
